server:
  port: ${SERVER_PORT:8082}

spring:
  data:
    mongodb:
      uri: ${SUBJECT_MONGODB_URI:mongodb+srv://shimarailshani:t8PKnvDa9KVrEVbH@mini-pos.kg3ltyq.mongodb.net/subject_service_db?retryWrites=true&w=majority&serverSelectionTimeoutMS=2000&connectTimeoutMS=2000&socketTimeoutMS=2000}
  cloud:
    config:
      import-check:
        enabled: false

management:
  health:
    mongodb:
      enabled: false

eureka:
  instance:
    hostname: localhost
    prefer-ip-address: false

info:
  app:
    name: Subject Service
    description: Manages subjects, departments, and curriculum entries in MongoDB

